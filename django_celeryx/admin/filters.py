"""Admin filters (state, task name, worker, queue)."""

from __future__ import annotations

# Additional filters beyond those in queryset.py will be added here.
# The primary filters (TaskStateFilter, WorkerStatusFilter) live in queryset.py
# alongside their respective mixins, matching the django-cachex pattern.
