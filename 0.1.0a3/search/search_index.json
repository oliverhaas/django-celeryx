{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Django CeleryX","text":"<p>Celery monitoring and management for Django admin. Like Flower, but embedded in your Django admin with htmx + Alpine.js for real-time updates.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Real-time task monitoring - Live task list with state, args, result, timing</li> <li>Worker management - View worker status, pool info, queues, config</li> <li>Control actions - Revoke/terminate tasks, shutdown/restart workers, manage pools and queues</li> <li>Metrics dashboard - Throughput, latency percentiles, success/failure rates</li> <li>Broker overview - Queue names and message counts</li> <li>Django admin native - Looks and feels like Django admin, no separate service</li> <li>No npm/build step - htmx + Alpine.js included as static files</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>INSTALLED_APPS = [\n    # ...\n    \"django_celeryx.admin\",\n]\n</code></pre> <p>That's it. Start your Django server and navigate to the admin to see your Celery tasks and workers.</p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.12+</li> <li>Django 5.2+</li> <li>Celery 5.4+</li> </ul>"},{"location":"getting-started/installation/#install","title":"Install","text":"<pre><code>pip install django-celeryx\n</code></pre> <p>For django-unfold themed admin:</p> <pre><code>pip install django-celeryx[unfold]\n</code></pre>"},{"location":"getting-started/installation/#setup","title":"Setup","text":"<p>Add <code>django_celeryx.admin</code> to your <code>INSTALLED_APPS</code>:</p> <pre><code>INSTALLED_APPS = [\n    # ...\n    \"django_celeryx.admin\",\n]\n</code></pre> <p>For django-unfold, use <code>django_celeryx.unfold</code> instead.</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":""},{"location":"getting-started/quickstart/#basic-setup","title":"Basic Setup","text":"<ol> <li>Install django-celeryx:</li> </ol> <pre><code>pip install django-celeryx[celery]\n</code></pre> <ol> <li>Add to your Django settings:</li> </ol> <pre><code>INSTALLED_APPS = [\n    # ...\n    \"django_celeryx.admin\",\n]\n</code></pre> <ol> <li>Start your Django server and navigate to the admin. You'll see Tasks, Workers, Queues, and Registered Tasks sections.</li> </ol>"},{"location":"getting-started/quickstart/#configuration","title":"Configuration","text":"<p>Optional configuration via <code>CELERYX</code> dict in settings:</p> <pre><code>CELERYX = {\n    \"MAX_TASK_COUNT\": 100_000,\n    \"MAX_TASK_AGE\": 86400,\n    \"AUTO_REFRESH_INTERVAL\": 3,\n}\n</code></pre> <p>See Configuration for all options.</p>"},{"location":"reference/api/","title":"API Reference","text":"<p>Most of django-celeryx is used through the Django admin rather than imported. These are the pieces you may want to call directly.</p>"},{"location":"reference/api/#settings","title":"Settings","text":""},{"location":"reference/api/#django_celeryx.settings.CeleryXSettings","title":"<code>django_celeryx.settings.CeleryXSettings</code>  <code>dataclass</code>","text":"<p>Settings for django-celeryx.</p> <p>All settings are configured via the CELERYX dict in Django settings::</p> <pre><code>CELERYX = {\n    \"CELERY_APP\": \"myproject.celery.app\",\n    \"DATABASE\": \"celeryx\",  # or any Django DATABASES alias\n    ...\n}\n</code></pre>"},{"location":"reference/api/#django_celeryx.settings.get_db_alias","title":"<code>django_celeryx.settings.get_db_alias()</code>","text":"<p>Get the database alias for celeryx models.</p> <p>If DATABASE is not configured, auto-creates a dedicated 'celeryx' SQLite file database alongside the default database.</p>"},{"location":"reference/api/#task-control","title":"Task control","text":""},{"location":"reference/api/#django_celeryx.control.tasks","title":"<code>django_celeryx.control.tasks</code>","text":"<p>Task control actions: revoke, terminate, abort, apply, rate-limit, timeout.</p>"},{"location":"reference/api/#django_celeryx.control.tasks.revoke_task","title":"<code>revoke_task(task_id, *, terminate=False, signal='SIGTERM')</code>","text":"<p>Revoke a task, optionally terminating it.</p>"},{"location":"reference/api/#django_celeryx.control.tasks.abort_task","title":"<code>abort_task(task_id)</code>","text":"<p>Abort an abortable task.</p>"},{"location":"reference/api/#django_celeryx.control.tasks.apply_task","title":"<code>apply_task(name, args=None, kwargs=None)</code>","text":"<p>Send a task by name. Returns task ID.</p>"},{"location":"reference/api/#django_celeryx.control.tasks.set_rate_limit","title":"<code>set_rate_limit(task_name, rate, *, destination=None)</code>","text":"<p>Set rate limit for a task type.</p>"},{"location":"reference/api/#django_celeryx.control.tasks.set_time_limit","title":"<code>set_time_limit(task_name, *, soft=None, hard=None, destination=None)</code>","text":"<p>Set time limit for a task type.</p>"},{"location":"reference/api/#worker-control","title":"Worker control","text":""},{"location":"reference/api/#django_celeryx.control.workers","title":"<code>django_celeryx.control.workers</code>","text":"<p>Worker control actions: shutdown, restart pool, grow/shrink, autoscale, consumers.</p>"},{"location":"reference/api/#django_celeryx.control.workers.reply_error","title":"<code>reply_error(replies)</code>","text":"<p>Return the first error a worker reported, or None if all replies are ok.</p> <p>Control commands are broadcasts. Without reply=True a command that the worker rejects (shrinking a busy pool, for example) looks like a success.</p>"},{"location":"reference/api/#django_celeryx.control.workers.shutdown_worker","title":"<code>shutdown_worker(hostname)</code>","text":"<p>Shutdown a worker.</p>"},{"location":"reference/api/#django_celeryx.control.workers.pool_restart","title":"<code>pool_restart(hostname)</code>","text":"<p>Restart a worker's pool.</p>"},{"location":"reference/api/#django_celeryx.control.workers.pool_grow","title":"<code>pool_grow(hostname, n=1)</code>","text":"<p>Grow a worker's pool by n processes.</p>"},{"location":"reference/api/#django_celeryx.control.workers.pool_shrink","title":"<code>pool_shrink(hostname, n=1)</code>","text":"<p>Shrink a worker's pool by n processes.</p>"},{"location":"reference/api/#django_celeryx.control.workers.autoscale","title":"<code>autoscale(hostname, max_concurrency, min_concurrency)</code>","text":"<p>Set autoscale limits for a worker.</p>"},{"location":"reference/api/#django_celeryx.control.workers.add_consumer","title":"<code>add_consumer(hostname, queue)</code>","text":"<p>Add a queue consumer to a worker.</p>"},{"location":"reference/api/#django_celeryx.control.workers.cancel_consumer","title":"<code>cancel_consumer(hostname, queue)</code>","text":"<p>Cancel a queue consumer from a worker.</p>"},{"location":"reference/api/#event-listener","title":"Event listener","text":""},{"location":"reference/api/#django_celeryx.state.events.start_event_listener","title":"<code>django_celeryx.state.events.start_event_listener()</code>","text":"<p>Start the event listener thread (idempotent).</p>"},{"location":"reference/api/#django_celeryx.state.events.stop_event_listener","title":"<code>django_celeryx.state.events.stop_event_listener(timeout=5.0)</code>","text":"<p>Stop the event listener thread and wait for it to finish.</p>"},{"location":"reference/api/#persistence","title":"Persistence","text":""},{"location":"reference/api/#django_celeryx.state.persistence.cleanup_old_tasks","title":"<code>django_celeryx.state.persistence.cleanup_old_tasks()</code>","text":"<p>Delete tasks older than MAX_TASK_AGE and enforce MAX_TASK_COUNT. Returns count deleted.</p>"},{"location":"reference/api/#django_celeryx.state.persistence.ensure_tables","title":"<code>django_celeryx.state.persistence.ensure_tables()</code>","text":"<p>Migrate the auto-configured SQLite database on startup.</p> <p>This only runs for the dedicated <code>celeryx.sqlite3</code> database that the package creates for you. When <code>CELERYX[\"DATABASE\"]</code> names one of your own aliases, migrating is your job::</p> <pre><code>python manage.py migrate django_celeryx --database=&lt;alias&gt;\n</code></pre> <p>Migrating a shared database from <code>AppConfig.ready()</code> would run once per worker process on every boot, which races and can deadlock on backends that take DDL locks.</p>"},{"location":"reference/api/#metrics","title":"Metrics","text":""},{"location":"reference/api/#django_celeryx.metrics.metrics_view","title":"<code>django_celeryx.metrics.metrics_view(request)</code>","text":"<p>Django view that serves Prometheus metrics in text format.</p>"},{"location":"reference/changelog/","title":"Changelog","text":""},{"location":"reference/changelog/#010a3","title":"0.1.0a3","text":""},{"location":"reference/changelog/#security","title":"Security","text":"<ul> <li>Control actions now require the <code>change</code> permission. Custom admin views only   ran through <code>AdminSite.admin_view()</code>, which checks <code>is_staff</code> and nothing else,   so any staff user could revoke tasks, shut down workers, and send new tasks.   Task and Worker gained a <code>change</code> permission; <code>view</code> is monitoring-only.</li> <li>Fix stored XSS on the dashboard. Chart data was rendered with <code>|safe</code>, so a   task name or worker hostname containing <code>&lt;/script&gt;</code> could inject markup. The   data now goes through the <code>json_script</code> filter.</li> </ul>"},{"location":"reference/changelog/#fixed","title":"Fixed","text":"<ul> <li>The event listener stopped reconnecting after the first broker failure. The   flush timer shared the listener's stop event, so shutting it down also   cancelled the reconnect loop.</li> <li>The unfold app shipped no migrations and could not find its templates, so   <code>migrate django_celeryx</code> failed and every page raised <code>TemplateDoesNotExist</code>.</li> <li>The unfold dashboard raised <code>AlreadyRegistered</code> by importing the standard   admin module for its filters. The filters moved to <code>admin/filters.py</code>.</li> <li>Detail views passed a hand-built dict as <code>opts</code>, which broke unfold template   tags that read <code>opts.app_label</code> in Python. They pass the model <code>_meta</code> now.</li> <li><code>override_settings(CELERYX=...)</code> left the cached settings dataclass in place.   A <code>setting_changed</code> receiver now invalidates it.</li> <li><code>get_db_alias()</code> no longer rebuilds the settings dataclass on every database   access.</li> <li>The stand-in querysets silently ignored unsupported <code>filter()</code> lookups and   returned unfiltered rows. They raise <code>NotImplementedError</code> instead.</li> <li>Task and worker list queries are capped at 1000 rows with a warning, instead   of loading the whole table into memory.</li> <li>Workers that were killed, crashed, or died while the listener was down stayed   \"online\" forever. Celery only emits worker-offline on a graceful shutdown, so   liveness now falls back to heartbeat age.</li> <li>Worker control commands were sent without <code>reply=True</code>, so a command the   worker rejected (shrinking a busy pool) was reported as a success.</li> <li>The worker pool tab showed <code>max-concurrency</code>, which does not change on grow or   shrink, making both look like no-ops. It shows the live process count now.</li> </ul>"},{"location":"reference/changelog/#added","title":"Added","text":"<ul> <li><code>AUTO_MIGRATE</code> setting to control the startup migration.</li> <li>CI runs mypy and ty on publish, and the unfold test job is enabled again.</li> <li>Tests for the event listener, worker and task control, permission   enforcement, and dashboard escaping. Coverage 67% to 78%.</li> <li>API reference documents the public symbols instead of a bare module dump.</li> </ul>"},{"location":"reference/changelog/#changed","title":"Changed","text":"<ul> <li>Update dev dependencies (mypy 2.3.1, ruff 0.16.4, ty 0.0.73, pytest 9.1.1,   django-stubs 6.1.0, and others). prometheus-client is now a dev dependency, so   the metrics tests actually run.</li> <li>Document the permission model, the security caveats around task arguments,   and the unauthenticated metrics endpoint.</li> </ul>"},{"location":"reference/changelog/#010a2","title":"0.1.0a2","text":"<ul> <li>Production-ready audit: fix type errors, config alignment, documentation accuracy</li> <li>Fix <code>retried</code> timestamp silently dropped (field name mismatch with DB model)</li> <li>Fix <code>ENABLE_EVENTS</code> setting not being checked before broadcasting</li> <li>Fix <code>persist_worker_event</code> allowing empty strings to overwrite values</li> <li>Fix <code>_format_timestamp</code> treating <code>0.0</code> as no-data</li> <li>Fix <code>order_by</code> ignoring multi-field sort and sorting numeric fields as strings</li> <li>Fix Prometheus metrics dropping <code>runtime=0.0</code> events</li> <li>Fix signal handler accumulating duplicates (add <code>dispatch_uid</code>)</li> <li>Fix unfold <code>LiveUpdateMixin</code> not stripping <code>live</code> param from request</li> <li>Fix unfold <code>DashboardAdmin</code> loading full table, missing <code>LiveUpdateMixin</code> and <code>show_facets</code></li> <li>Validate <code>CELERY_APP</code> dotted path, warn on unknown settings keys</li> <li>Thread-safe <code>_LazySettings</code> with double-checked locking</li> <li>Update dev dependencies (django-stubs 6.0.2, mypy 1.20, ruff 0.15.9, ty 0.0.29)</li> <li>Fix documentation: \"in-memory SQLite\" \u2192 \"dedicated SQLite file\", accurate dashboard/broker docs</li> <li>Document <code>PROMETHEUS_PREFIX</code> setting</li> <li>Remove stale planning docs</li> </ul>"},{"location":"reference/changelog/#010a1","title":"0.1.0a1","text":"<ul> <li>Initial alpha release</li> <li>Event-driven task and worker monitoring</li> <li>Django admin integration with unmanaged models</li> <li>Real-time UI with htmx + idiomorph</li> <li>Worker control actions</li> <li>Metrics dashboard</li> <li>Prometheus metrics endpoint</li> <li>Configurable metric prefix (<code>PROMETHEUS_PREFIX</code>)</li> </ul>"},{"location":"user-guide/broker/","title":"Broker","text":"<p>The broker view shows queue information from connected workers.</p>"},{"location":"user-guide/broker/#queue-list","title":"Queue List","text":"<ul> <li>Queue name</li> <li>Exchange name</li> <li>Routing key</li> <li>Consumer count (number of workers consuming from this queue)</li> </ul> <p>Queue data is fetched via <code>celery.control.inspect().active_queues()</code>.</p>"},{"location":"user-guide/configuration/","title":"Configuration","text":"<p>All settings are optional and configured via the <code>CELERYX</code> dict in your Django settings.</p> <pre><code>CELERYX = {\n    # Celery app (dotted path). Auto-detected if None.\n    \"CELERY_APP\": None,\n    # Database alias for storing task/worker state.\n    # None = auto-configured dedicated SQLite file (celeryx.sqlite3).\n    # Set to a DATABASES alias to use your own database (e.g. \"default\", \"celeryx\").\n    \"DATABASE\": None,\n    # Maximum age of stored task records in seconds (default 24h).\n    \"MAX_TASK_AGE\": 86400,\n    # Maximum number of task records in the database.\n    # Oldest records are pruned when this limit is exceeded.\n    \"MAX_TASK_COUNT\": 100_000,\n    # Set to False to disable monitoring on this instance.\n    # Useful in multi-pod deployments where only one pod should monitor.\n    \"ADMIN_ENABLED\": True,\n    # Start the event listener automatically on Django startup.\n    \"EVENT_LISTENER_AUTOSTART\": True,\n    # Periodically broadcast enable_events to workers.\n    \"ENABLE_EVENTS\": True,\n    # Run the celeryx migrations on startup. Only applies to the auto-configured\n    # SQLite file; when DATABASE is set you run migrations yourself.\n    \"AUTO_MIGRATE\": True,\n    # Timeout for celery.control.inspect() calls.\n    \"INSPECT_TIMEOUT\": 1.0,\n    # Seconds between htmx live update polls (0 to disable).\n    \"AUTO_REFRESH_INTERVAL\": 3,\n    # Visible columns on the task list page.\n    \"TASK_COLUMNS\": [\n        \"name\",\n        \"uuid\",\n        \"state\",\n        \"worker\",\n        \"received\",\n        \"started\",\n        \"runtime\",\n    ],\n    # Use relative timestamps (e.g. \"5 minutes ago\") instead of absolute.\n    \"NATURAL_TIME\": False,\n    # Prometheus metric name prefix. Set to \"flower\" for Flower drop-in compat.\n    \"PROMETHEUS_PREFIX\": \"django_celeryx\",\n}\n</code></pre>"},{"location":"user-guide/configuration/#database-persistence","title":"Database Persistence","text":"<p>By default, django-celeryx stores all state in an auto-configured SQLite file database (<code>celeryx.sqlite3</code>) placed alongside your default database. To use a different database backend:</p> <pre><code># Option 1: Use an explicit database\nDATABASES = {\n    \"default\": {...},\n    \"celeryx\": {\n        \"ENGINE\": \"django.db.backends.sqlite3\",\n        \"NAME\": BASE_DIR / \"celeryx.sqlite3\",\n    },\n}\n\nCELERYX = {\n    \"DATABASE\": \"celeryx\",\n}\n\n# Required: add the database router\nDATABASE_ROUTERS = [\"django_celeryx.db_router.CeleryXRouter\"]\n</code></pre> <p>Then run migrations:</p> <pre><code>python manage.py migrate --database=celeryx\n</code></pre> <p>Setting <code>DATABASE</code> turns off the startup migration, so this step is on you. With the default auto-configured SQLite file, django-celeryx migrates on startup instead. Set <code>AUTO_MIGRATE</code> to <code>False</code> if you would rather run it yourself.</p>"},{"location":"user-guide/configuration/#multi-pod-deployments","title":"Multi-Pod Deployments","text":"<p>In production, you typically run multiple Django instances. Only one should run the event listener:</p> <pre><code># On the monitoring pod:\nCELERYX = {\n    \"ADMIN_ENABLED\": True,\n    \"DATABASE\": \"celeryx\",\n}\n\n# On all other pods:\nCELERYX = {\n    \"ADMIN_ENABLED\": False,\n}\n</code></pre>"},{"location":"user-guide/configuration/#permissions","title":"Permissions","text":"<p>The Task and Worker models carry both a <code>view</code> and a <code>change</code> permission:</p> Permission Grants <code>django_celeryx.view_task</code> Read the task list and task detail pages <code>django_celeryx.change_task</code> Revoke, terminate, and send tasks <code>django_celeryx.view_worker</code> Read the worker list and worker detail pages <code>django_celeryx.change_worker</code> Pool and consumer controls, rate and time limits <p>Queue, RegisteredTask, and Dashboard are read-only and only have <code>view</code>.</p> <p>Django's admin only checks <code>is_staff</code> on custom admin views, so django-celeryx checks these permissions itself. A user with just the <code>view</code> permissions gets a monitoring-only interface: the control forms and the Send Task link are not rendered, and posting to those endpoints returns 403.</p>"},{"location":"user-guide/configuration/#security-notes","title":"Security Notes","text":"<p>Task <code>args</code>, <code>kwargs</code>, and <code>result</code> are stored as text and are searchable from the task list. If your tasks take credentials, tokens, or personal data as arguments, anyone with <code>view_task</code> can read them. Restrict the permission, or keep secrets out of task arguments.</p> <p>The Prometheus endpoint at <code>/celeryx/metrics/</code> has no authentication. It exposes task names, worker hostnames, and queue depths. Restrict it at the network or reverse-proxy layer rather than exposing it publicly.</p>"},{"location":"user-guide/dashboard/","title":"Dashboard","text":"<p>The metrics dashboard provides an overview of your Celery cluster's performance.</p>"},{"location":"user-guide/dashboard/#charts","title":"Charts","text":"<ul> <li>Throughput - Succeeded/failed tasks per time bin (Chart.js)</li> <li>Slowest tasks - Average runtime with standard deviation by task type</li> <li>Failure rate - Failure percentage by task type</li> <li>Worker load - Task counts per worker</li> <li>Top tasks - Most frequently executed task types</li> </ul>"},{"location":"user-guide/dashboard/#time-periods","title":"Time Periods","text":"<p>Filter the dashboard by time period:</p> <ul> <li>Today - Last 24 hours</li> <li>Last 7 days</li> <li>Last 30 days</li> </ul>"},{"location":"user-guide/dashboard/#filters","title":"Filters","text":"<ul> <li>Queue - filter by routing key</li> <li>Worker - filter by worker hostname</li> </ul>"},{"location":"user-guide/tasks/","title":"Tasks","text":"<p>The task list provides a real-time view of all Celery tasks.</p>"},{"location":"user-guide/tasks/#task-list","title":"Task List","text":"<p>Auto-refreshing table showing tasks with configurable columns:</p> <ul> <li>name, uuid, state, args, kwargs, result</li> <li>received, started, runtime</li> <li>worker, exchange, routing_key</li> <li>retries, revoked, exception, expires, eta</li> </ul>"},{"location":"user-guide/tasks/#task-states","title":"Task States","text":"<p>Color-coded states: PENDING, RECEIVED, STARTED, SUCCESS, FAILURE, RETRY, REVOKED.</p>"},{"location":"user-guide/tasks/#search","title":"Search","text":"<p>Structured search with prefix support:</p> <ul> <li>Free text across all fields</li> <li><code>state:FAILURE</code> - Filter by state</li> <li><code>result:value</code> - Search results</li> <li><code>args:value</code> - Search arguments</li> <li><code>kwargs:key=value</code> - Search keyword arguments</li> </ul>"},{"location":"user-guide/tasks/#actions","title":"Actions","text":"<ul> <li>Revoke - Cancel a pending task</li> <li>Terminate - Kill a running task</li> <li>Abort - Abort an abortable task</li> </ul>"},{"location":"user-guide/workers/","title":"Workers","text":"<p>The worker list shows all active Celery workers with their status.</p>"},{"location":"user-guide/workers/#worker-detail-tabs","title":"Worker Detail Tabs","text":"<ul> <li>Pool - Pool type, concurrency, processes. Controls: grow/shrink, autoscale</li> <li>Queues - Active queues. Controls: add/cancel consumer</li> <li>Tasks - Processed counts, active/scheduled/reserved/revoked tasks</li> <li>Limits - Rate limits and timeouts. Controls: set rate limit, set timeout</li> <li>Config - Full worker Celery configuration</li> <li>Stats - System resource usage, broker connection info</li> </ul>"},{"location":"user-guide/workers/#control-actions","title":"Control Actions","text":"<ul> <li>Shutdown worker</li> <li>Restart worker pool</li> <li>Grow/shrink pool</li> <li>Set autoscale limits</li> <li>Add/cancel queue consumer</li> <li>Set rate limits and time limits</li> </ul>"}]}